<UserControl
    x:Class="TheAnimeFetcher.Controls.MangaControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TheAnimeFetcher.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="400"
    Width="300">

    <Grid>
        <Image Source="{x:Bind Manga.manga_image_pathHighRes}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Stretch="Fill"/>
        <Grid Background="Gray" HorizontalAlignment="Stretch" VerticalAlignment="Bottom">
            <TextBlock Text="{x:Bind Manga.manga_title}" FontSize="17" />
        </Grid>
    </Grid>
</UserControl>
